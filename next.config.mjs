/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "static.vecteezy.com",
      "lh3.googleusercontent.com",
      "i.pinimg.com",
    ],
  },
};

export default nextConfig;
